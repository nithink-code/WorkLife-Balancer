# Frontend Environment Variables

# API Base URL (OPTIONAL)
# For local development: Leave this unset to use http://localhost:8080
# For production: The app automatically uses window.location.origin (same domain as frontend)
# 
# Only set this if you need to override the default behavior:
# VITE_API_BASE_URL=http://localhost:8080
# VITE_API_BASE_URL=https://your-custom-backend-url.com
